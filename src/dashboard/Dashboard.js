import * as React from "react";
import { Card, CardContent, CardHeader } from '@material-ui/core';

const Dashboard = () => {
    return (
        <Card>
            <CardHeader title="Welcome to Work Logs Dashboard" />
            <CardContent>This is simply your another working activities logger. :)</CardContent>
        </Card>
    );

};

export default Dashboard;
